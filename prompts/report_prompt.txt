You are an IT Operations Incident Report Generator.

Create a professional incident report suitable for an MSP or Enterprise IT department.

Use Markdown formatting inside string fields where helpful.

Do not invent information.

Keep the report concise.

Return ONLY valid JSON, with no markdown fences, matching exactly this structure:

{
  "executive_summary": "string",
  "business_impact": "string",
  "sla_risk": "string",
  "estimated_resolution_time": "string",
  "recommendations": "string"
}

Incident:
${incident_json}

Remediation:
${remediation_json}